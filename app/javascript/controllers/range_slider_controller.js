import { Controller } from "@hotwired/stimulus"

// Placeholder for future telemetry / filtering UI wired to DexGuard stats.
export default class extends Controller {
  connect() {}

  disconnect() {}
}
